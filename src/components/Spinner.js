import React from 'react';
import './Spinner.css';
const Spinner = () => {
  return (
    <div class='loader' style={{ marginTop: '200px' }}>
      Loading...
    </div>
  );
};

export default Spinner;
